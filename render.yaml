services:
  - type: web
    name: movie-catalog
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
    disk:
      name: sqlite-data
      mountPath: /var/data
      sizeGB: 1